package zuo2023;

/**
 * 给定一个数组arr，长度为n，最多可以删除一个连续子数组，
 * 求剩下的数组，严格连续递增子数组的最大长度
 * n <= 10^6
 */
public class Code14_ {

    public int maxLen(int[] arr) {
        int n = arr.length;
        int[] dp = new int[n + 1];
        dp[0] = 1;

        return 0;
    }
}
